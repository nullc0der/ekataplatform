import request from 'superagent'

var lastLocation = '/';

const debug = require('debug')('ekata:react-router-utils');

export default function getRouteUtils(store, req, res){
	var Utils = {};
	return Utils
}